# Native helper DLL for Software Device (end users)

`VDisplayNative.dll` is required by VDisplay.Service to start the IDD software device.

Copied automatically by Helper before service start from this folder.

Developers: `.\scripts\build-native.ps1`
